# 第六轮升级：环境材质与亭子近景细节

基于本次上传的第五轮完整项目，保留原有场景构图、诗文与操作方式。第五轮记录见 [docs/UPGRADE_V5.md](./docs/UPGRADE_V5.md)。本轮未更新原 GitHub 在线站点。

## 环境材质

- **砖石与铺地**：77 块亭内铺石、石桥桥板、园路和侧石采用真实内收倒角，保持原有边界和路面净空；逐块偏色、矿物颗粒、磨边与近地潮痕打破同一张纹理铺满全场的效果。石灰墙单独使用抹灰纹理。
- **树木**：换用独立的桃树树皮源图，保留横向皮孔、细裂隙和不规则鳞片；粗枝根部增加渐变连接，统一树皮物理尺度。4752 片叶保持原数量，分为三种卷曲轮廓，增加叶宽和朝向差异，细化叶脉色差。
- **青瓦**：换用独立的灰青陶土源图，每片瓦读取不同的纹理区域，保留受控的烧制色差、搭接痕与切口浅色。原有 9526 片瓦保持不变，垂脊的连续管形改为 228 节实体搭接陶瓦。

## 亭子

- 台基补充 90 块独立砌石，石块之间有留缝，边缘实际倒角。
- 木构源图包含自然木纤维、木节、细孔与色差；木柱、梁、斗拱和椽子按构件长轴铺设木纹。环梁在转角处按每段梁校正方向。
- 檐下补齐细板缝，192 个可见椽头加入独立年轮端面；透榫木销 12 个、斗拱出榫端头 36 个。
- 坐凳加双拼木板与木销，匾额保留原题字，增加实体边框与旧铜钉；金属采用较克制的反射。
- 材质逐件颜色保留到静态合批和 GLB 的标准顶点颜色中，避免只在网页中出现。

## 保持原样

草地 57601 簇、345606 片草叶的几何、实例矩阵与颜色逐字节对照上传版一致。昼夜光照参数、水体实现、轻纱动画、地形、手机样式和相机控制保持不变，Mac 启停脚本和依赖锁文件保留。校验记录见 `docs/model-audit.json`。

## 资源预算

| 建筑庭园指标 | 上传版 V5 | 本轮 V6 |
| --- | ---: | ---: |
| 三角面（按实例展开计数） | 2,459,624 | 2,491,436（+1.29%） |
| 几何与实例缓冲 | 15.67 MiB | 17.82 MiB |
| 可绘制网格 / 材质 | 92 / 22 | 95 / 23 |
| 模型纹理数量 | 20 | 26 |
| 模型纹理含 mipmap 估算 | 12.00 MiB | 26.00 MiB |
| 随包轻量 GLB | 28.58 MiB | 32.99 MiB |

三张材质源图各为 1254² 像素，运行时使用 512² 的颜色、法线与粗糙度图；抹灰和木端面为 256²，轻纱和纸为 128²。较细纹理增加约 14 MiB 的模型贴图显存估算，不含水面、阴影、环境贴图、帧缓冲和浏览器开销；不能据此推断手机 FPS。

## 验证与适用范围

29 项自动测试通过，覆盖原有启动、接缝、动态部件、实例颜色和导出流程，以及新增铺石闭合、倒角边界、真实资源加载、重复贴图接缝和纹理预算。生产构建完成。

完整轻量与兼容 GLB 的最终校验结果见 `docs/model-audit.json` 及相应的 `*-validation.json`。导出中的颜色、标准法线、粗糙度、木端面、纸灯发光与织纹比例随文件内嵌；兼容格式继续可通过原命令生成。

已经检查三张材质源图与生成的程序纹理；本轮未进行浏览器 WebGL 整体画面验收、真实 Mac/Safari 或手机帧率实测。材质源图由 image_gen 生成，法线是经过高通处理的微表面近似，并非摄影测量的实测高度图。GLB 水面仍为静态 PBR 近似，各查看器的环境光和色调映射会影响最终显示。

## 使用

下载并解压 `changyi-pavilion-v6.zip`，Mac 双击 `Start-Mac.command`。启动方式与原项目一致；首次运行需要安装依赖。建议先使用“青瓦重檐”和“檐下听风”视角观察瓦口、脊瓦、台基、斗拱、椽头和匾框。

压缩包包含源码、三张材质源图、已构建的 `dist`、轻量 GLB、升级说明和验证记录，不含依赖目录或临时文件。兼容 GLB 体积较大，不放入项目包，可运行 `npm run export:model -- --compatible` 生成。

生成源图的完整提示词见 `docs/MATERIAL_PROMPTS_V6.txt`；工具采用内置 image_gen，最终资源位于 `public/textures/timber-albedo-v6.png`、`public/textures/peach-bark-albedo-v6.png` 和 `public/textures/blue-clay-albedo-v6.png`。
