# 长衣亭

为张依婷创作的原创六角重檐亭阁，以及可交互的网页展示。诗文保留全部八句，网页中不显示诗题。

**在线预览**：<https://yyh-0428.github.io/changyi-pavilion/>（推送到 `main` 后由 GitHub Actions 自动构建部署）

## 本轮写实升级

木构、树皮、石材和青瓦改用独立的颜色、法线和粗糙度纹理；轻纱补充细织纹，灯笼增加纸纤维和沿骨架变化的发光。主要木石构件增加内收倒角，木梁端面、柱身与台基的纹理比例重新校正。

夕照的天空太阳、投影和室外环境反射使用一致的方向；月夜主光对齐月亮。网页与 GLB 共用昼夜光照参数，导出保留材质纹理、织纹比例和灯笼发光。

网格仍为 92 个，三角面较上一轮增加 0.11%；模型纹理含 mipmap 的估算占用从 12.75 MiB 降至 12.00 MiB。手机适配与 Mac 启动脚本保持原样。完整指标和验证范围见 [UPGRADE_NOTES.md](./UPGRADE_NOTES.md)。

## Mac 快捷启动

解压完整项目后，双击 **Start-Mac.command**；首次自动安装依赖并打开浏览器。结束时按 **Control+C** 或双击 **Stop-Mac.command**。缺少 Node.js 时会打开官方下载页，建议安装 24 LTS 后重试。详见 [MAC_QUICKSTART.md](./MAC_QUICKSTART.md)。

启动脚本支持中文和空格路径、端口占用后自动换端口、重复启动复用服务，以及按项目停止；保留原有手机界面和操作方式。

## 运行

```sh
npm install
npm run dev -- --host 127.0.0.1
```

生产构建：`npm run build`。发布 `dist` 目录即可。

模型校验：`npm test`。重新生成仓库中的 GLB：`npm run export:model`，再运行 `npm run verify:export` 验证重新导入。离线生成使用开发依赖 `@napi-rs/canvas`，不会进入网页代码。

历轮模型升级、Mac 启动方式、性能口径和验证范围见 [UPGRADE_NOTES.md](./UPGRADE_NOTES.md)。

## 场景

- Three.js PBR 木构、青瓦、石台、栏杆、斗拱、题字、轻纱和灯笼。重檐屋顶采用 9,450 片随曲面收分的实体板瓦/筒瓦，另有月洞门 76 片盖瓦，保留搭接厚度、瓦当和材质差异。
- 增加环梁、斜撑、三层藻井梁架、檩条与椽子。三株桃树包含 1,044 段渐细枝干、4,752 片有叶脉和卷曲的桃叶，以及逐枝分布的五瓣花。
- 屋面补齐 0.055 场景单位厚的望板、檐下表面和封边，瓦当方向跟随檐口曲率；柱础与柱身具有连续收分轮廓，斗拱增加曲线承托与坐斗。地坪改为 77 块沿六角边界裁切的错缝铺石。
- 桥头连接实体岸地、连续曲折石径、月洞门与石灯，亭内设双人茶席。桥下增加拱形承托石和两端桥台。
- 桥下两侧券石按拱线法向切分；桥板和园路按曲线裁切，桥尾采用统一断面接缝，月洞门开口避开路面与路缘。
- 草地采用世界坐标地表纹理与 57,601 簇实体草丛，共 345,606 片弯曲草叶，覆盖岛上和桥头庭园。草丛按空间分块实例化，具备高度/颜色变化及轻微风动，避让石径、台基、门墙、树根和石灯。
- 水面采用 Three.js Reflector/Refractor 的独立反射与折射视图，按窗口比例生成最高 2048 像素的 HDR 反射图。深度纹理重建湖底位置，并按光路长度进行 Beer-Lambert 吸收和菲涅耳混合；两个几何波与三层流动法线形成水面细节。
- 配套渐深的浅滩和湖底，近岸透射、深水吸收与灯光倒影使用同一套水体材质。反射相机处理了偏移构图，避免倒影错位和画面边缘拉伸。
- 檐角收头和屋顶宝顶改用低矮的青灰陶质构件，去除外露的金黄色翘角。顶部、底部界面使用低不透明度玻璃底色，菜单和诗笺保留适当对比度。
- 夕照与月夜、五个观景视角、自动环绕、诗笺、图片导出和 GLB 场景快照导出。细节视角隐藏大标题，避免遮挡模型。
- 建筑与植物使用自建几何和程序纹理；水面法线采用 Three.js 官方示例贴图，已下载到本地，运行时无需外部素材或图片服务。
- 桌面与手机分别限制像素比、反射贴图和阴影贴图尺寸。
- 保留全部瓦片与植被数量，将瓦片直线方向的冗余分段、细枝截面、叶片和草叶拓扑精简；静态构件采用索引合批，灯笼保留可动画的合批支点，轻纱以 GPU 变形并修正法线，收束轮廓和椭圆系环同步摆动。模型约 246 万三角面，几何与实例缓冲约 15.67 MiB，可绘制网格仍为 92 个。
- 匾额贴图直接复用原 GLB 内嵌字样，不依赖终端是否安装相同中文字体；草地、树木和瓦片在重复生成与离线导出时保持相同分布。

模型是原创艺术设计，不是特定历史建筑的测绘复原；水面是适合实时网页的光学近似，并非离线光线追踪或完整流体模拟。

## 文件

网页与命令行共用场景快照和导出流程，复制当前草叶、轻纱、灯笼姿态及材质，保留地形、湖底、两盏方向光、六盏点光和当前相机。凹凸细节使用共享的标准法线贴图，导出时补齐切线，不再依赖 `EXT_materials_bump`。

| 文件 / 菜单 | 用途 |
| --- | --- |
| `changyi-pavilion.glb` / 轻量场景 | 约 28.6 MiB，保留 `EXT_mesh_gpu_instancing`，需要查看器支持该扩展及实例颜色 |
| `changyi-pavilion-compatible.glb` / 兼容场景 | 约 128.2 MiB，实例展开为普通索引网格，颜色写入标准 `COLOR_0`，适合建模软件导入 |

兼容文件仅在执行 `npm run export:model -- --compatible` 后生成，因超过 GitHub 单文件大小限制不纳入 Git；验证使用 `npm run verify:export -- --compatible`。`npm run export:model -- --model-only` 可生成不带场景环境的轻量建筑庭园版本。

**导入后选择“当前视角”相机**。完整地形尺度大于亭阁，查看器自动框选全场景时，建筑会显得较小。水面保留导出时刻的两层几何波，材质是静态 PBR 近似；实时倒影、折射、深度吸收、程序天空、雾、环境照明、ACES 色调映射及相机偏移不属于可原样移植的通用 GLB 效果。其他查看器需要自己的环境光设置，无法承诺逐像素一致。月夜下载保留当时可见的月亮与星点。

兼容导出的体积和临时内存开销只发生在点击导出时；日常网页继续使用实例化，不加载这个大文件。低内存手机优先使用轻量场景。移动端 CSS、相机和手势设置保留，只在原留存菜单增加一个兼容导出选项。

网页实拍截图（`preview-*.png`）与画布导出图（`changyi-render.png`）体积较大，未纳入本仓库，仅保留在本地。

## 许可

代码以 [MIT](./LICENSE) 许可发布；`public/textures/` 内的水面法线贴图来自 Three.js 官方示例（MIT），详见 [CREDITS.txt](./public/textures/CREDITS.txt)。
