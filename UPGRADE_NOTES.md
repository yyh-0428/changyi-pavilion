# V12：接缝、草地与水下锦鲤修复

五面栏杆横枋与压顶接入木柱，花格端部接实，交叉处使用连续实体；转角坐板按共同接面收口，补上靠背联结撑，并调整牙条、灯笼吊杆与垂穗的接点。

草地改为按真实地表边界铺设，补齐岸边斜坡上的草，水下坡面使用湿土色。鱼体增加凹入嘴部、平滑背部接缝、收敛的鱼鳍轮廓和透光鳍膜，调整水下光泽与背腹颜色。原反射边缘修正、浅滩几何和 V11 渲染优化保留。

验证、开销、当前预览与启动方式详见 `docs/DETAIL_FIX_V12.md`；以下为历史记录。

# V11：保持模型显示，减少每帧重复工作

- 水面使用保守的上下分区：完整位于水面一侧的对象只从原本会将它彻底裁掉的通道中排除。主画面继续绘制全部原可见模型；阴影继续每帧更新。
- 草丛的范围覆盖全部风动位移、每个实例的旋转与缩放，并保留 2 厘米裁切余量。灯笼、轻纱、穿过水面的地形和未知变形对象沿用共享通道。
- 主画面、倒影和折射共用本帧的场景世界矩阵，完整场景遍历从每帧 3 次降至 1 次，相机和阴影矩阵继续自动更新。
- 加载阶段异步预编译主画面与 HDR 水面两类材质程序；浏览器支持时使用 Three.js 的并行编译扩展。水面更新频率与分辨率保持原样。
- 5 个静止/动画状态的模型缓冲、贴图像素、材质和姿态数据与 V10 指纹一致；32 种机位、设备和光照组合中，主画面和阴影绘制对象一致。
- 默认夕照全景每帧提交三角面约减少 24%；这是 CPU 按实际相机及裁切规则计算的提交量，不是手机帧率测量。模型精度、鱼数、材质、像素比和动画频率均未降低。
- 17 项针对性回归、4 个原始 GLSL ES 3.00 程序编译链接及生产构建通过。未重新生成 GLB 或视频。

详细数值、算法边界和复核命令见 `docs/PERFORMANCE_V11.md`，完整记录见 `docs/PERFORMANCE_AUDIT_V11.json`。

# V10：恢复观亭首页、清晰近景与亭子细节

- 初始视角恢复“临水观亭”，沿用原电脑与手机相机位置，保留顶部观鲤入口。
- 原指数雾会使更远的手机视角显著泛白。改为从 68 米开始的距离雾，近处亭子保持清晰，夕照/月夜的远岸继续逐渐融入环境。
- 五面坐凳补齐 20 根落地榫腿、10 根穿带承托、10 根牙条和 10 根横枨。
- 五面栏杆增加倒角压顶；匾框改为四根闭合攒角边框，细倒棱不遮挡原题字。
- 额枋增加 12 根贴合梁面的细木线脚；曲线雀替修正顺纹贴图尺度。
- 新构件使用独立表面种子，避免改变既有树木、砖石的色差和贴图偏移；继续合并到原材质批次。
- 12 项针对性检查通过，4 个完整 GLSL ES 3.00 程序编译与链接通过，生产构建成功。未重复导出 GLB 或生成视频。

详细几何开销、手机雾感来源和验证范围见 `docs/PAVILION_V10.md`。

# V9.2 历史修复：鱼材质编译失败

用户浏览器日志明确指出 `patch` 是保留字，导致锦鲤片元着色器编译失败。已将变量改为 `patchNoise` 并更新程序缓存键。图案公式、颜色、模型、鱼数和游动模拟均保留。

此前离线检查把 `#version 300 es` 转成桌面 `#version 330`，因此漏掉了这一语言差异。现已去掉版本转换，并新增只编译、不生成场景或视频的检查脚本。原错误可复现，修复后的鱼材质基础/环境阴影雾变体、湖底和水面共 4 个程序按 GLSL ES 3.00 编译与链接通过；生产构建成功。

详细记录见 `docs/SHADER_FIX_V9_2.md` 与 `docs/SHADER_AUDIT_V9_2.json`。本轮不重复导出 GLB、生成视频或运行与此次修改无关的模型验证。

# V9.1 历史修复：鱼群启动与观景入口

- 首帧直接填充全部鱼体，不等待后台线程下载完成。
- 补齐线程超时、消息解码错误与消息发送失败接管，保留最近鱼体姿态。
- 使用折射相机专属图层绘制水下鱼群，省去全局可见性的逐帧切换。
- 首页直接进入观鲤近景，顶部增加“观鲤 / 观亭”；手机不再沿用其他建筑视角的 1.65 倍拉远。
- 更新旧版 Mac 文件名和启动说明，去除首页文档中容易误开的旧在线预览入口。
- 14 项针对性测试通过；本次仍不生成、不打包 GLB，不重复生成预览视频。详细原因、验证边界与文件对应见 `docs/FISH_VISIBILITY_V9_1.md`。

# V9 历史升级：锦鲤鱼群与实时游动

本轮基于 V8 加入水下鱼群，按要求省去预导出的 GLB，未重复执行建筑模型导出。V8 说明保存在 `docs/UPGRADE_V8.md`。

## 鱼的外观

- 桌面 120 条，手机 96 条，分布在亭岛与桥头周围的可游水域。
- 以暖白朱红、暖白香槟金、白底红顶三组配色为主，每条鱼的斑纹种子、色彩明度、体长、体宽和体高略有差异。
- 鱼体具备圆润头部、双眼、鳃盖线、口沿与两条口须；包含胸鳍、腹鳍、背鳍、臀鳍和分叉尾鳍。
- 鳞片有微小表面起伏与粗糙度变化，鳍膜带鳍条纹理；细纹在远处逐渐淡化，减轻闪烁。

## 游动与避让

- 各鱼有独立巡游目标及邻域反应，不沿同一段固定轨迹循环。
- 速度变化、身体侧摆、摆尾、胸鳍划动、俯仰和转弯倾斜联动。
- 60 Hz 固定步长模拟，限制加速度与转向速度；预测相邻鱼的最近接近距离，提前转弯或减速。
- 使用覆盖完整鱼体和摆动鱼鳍的保守包围体，进行鱼间非穿透约束，并避开岛岸、岸石、水面与湖底。
- 前后台切换时不会积累很长的补算队列；角度和摆尾相位循环保持精度。

这是为实时场景设计的运动与水下光学近似，不是完整流体力学求解，也不能据此宣称对所有设备、任意极端初始状态都有无条件保证。

## 渲染与性能

- 鱼群模拟放入 Web Worker；不支持 Worker 时沿用同一模拟器降级运行。
- 复用状态缓冲区，并对物理状态插值；显卡完成身体与鱼鳍变形，不逐条修改 CPU 顶点。
- 两档共享模型、一个 PBR 材质，最多两次鱼群绘制；近远切换具有距离滞回，保留个体花色与游动相位。
- 鱼只参与水下折射绘制，通过原有深度吸收和菲涅耳混合进入画面，避免在主画面及反射中重复绘制。
- 原建筑、月洞门、花木、茶席材质、水面分辨率和全局像素比保留。

本环境中，120 条鱼每个物理步平均约 0.37 ms，95 分位约 0.53 ms。这是共享 CPU 的模拟计算结果，不能替代真实浏览器或手机的帧率测量。详细记录见 `docs/FISH_AUDIT_V9.json`。

## 验证与使用

本轮 11 项针对性测试通过，包括鱼群连续游动两分钟、迎面避让、异常重叠恢复、不同帧率下的固定步长一致性、完整动画姿态的安全包围体、线程缓冲区传递，以及折射绘制的可见性和异常恢复；同时检查 V8 的茶壶、砖面 UV、阴影范围和贴图导出回归。

生产构建成功。4 个实际着色器程序在原生 OpenGL 中完成编译，包含带环境贴图、阴影和雾的鱼材质变体；完成 8 秒鱼群与水面专项预览。未执行完整网页的真实浏览器录屏或手机帧率实测。架构与范围见 `docs/FISH_ARCHITECTURE.md`。

解压后双击 `Start-Mac.command`，在观景菜单选择 **“临池观鲤”**。包内包含源码、素材、网页构建、游动视频、预览图与检查记录，不含任何 GLB/glTF 文件；鱼群运行不需要这些文件。原在线网站未发布更新。
