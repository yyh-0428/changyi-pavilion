# 第四轮升级：Mac 快捷启动与帘幕细节

对照第三轮 `feb66b3`，本轮分支 `upgrade/mac-launch-and-detail`。第三轮完整记录在 `docs/UPGRADE_V3.md`，第二轮记录在 `docs/UPGRADE_V2.md`。

## Mac 启动

新增 `Start-Mac.command` 和 `Stop-Mac.command`，具体使用方法见 [MAC_QUICKSTART.md](./MAC_QUICKSTART.md)。

启动脚本兼容 macOS 自带 Bash 3.2 的语法，支持中文、空格和特殊字符路径。自动查找系统、Homebrew、nvm、Volta、asdf、fnm 中满足 Vite 要求的 Node.js。缺少 Node 时打开官方安装页面；依赖安装由项目锁文件控制，并记录 Node 主版本、平台和 CPU 架构，避免跨平台复用错误的原生模块。

默认使用本机端口 4173；端口被占用时使用 Vite 实际选择的端口，待服务监听成功后再打开浏览器。重复双击会验证并复用同一项目的现有服务。退出支持 Control+C、关闭终端及 Stop-Mac.command；停止请求通过每个项目的随机会话令牌验证，不按进程名或端口批量结束其他程序。启动记录限制为当前用户读写，并禁止通过 Vite 读取。

Node 版本要求跟随当前锁定 Vite：`^20.19.0 || >=22.12.0`，推荐 Node.js 24 LTS。没有新增生产或开发依赖，也没有全局安装步骤。

## 模型与导出

- 帘幕重新设计为上下展开、系带处收束的连续轮廓，褶皱深度也在收束处变小。
- 原来的竖向大圆环改为贴合帘幕截面的椭圆系环，并补上小结头。系环与布料使用相同时间和相位摆动，网页着色器、CPU 系环和 GLB 烘焙参数集中在 `src/cloth.js`。
- 移除无贴图材质不用的 UV、无顶点颜色材质不用的颜色属性；单位范围 UV 改为归一化 16 位属性，较半精度浮点更精细，字节数不变。单位范围浮点顶点颜色压缩为 8 位，线性色彩通道误差不超过约 0.002。
- GLB 计算切线的阶段也按几何批次让出处理时间，减少兼容导出期间长时间占用主线程。
- 命令行导出先写临时文件、同步落盘、重新读取 SHA-256，核对成功后再替换旧文件。无效或截断的输入不会覆盖上一份完整导出。

| 建筑庭园指标 | 第三轮 | 第四轮 |
| --- | ---: | ---: |
| 三角形（展开计数） | 2,456,840 | 2,456,840 |
| 几何及实例缓冲 | 16,415,592 字节 | 16,260,931 字节（−0.94%） |
| 可绘制网格 | 92 | 92 |
| 独立几何 / 材质 / 模型纹理 | 92 / 22 / 11 | 92 / 22 / 11 |
| 轻量场景 GLB | 27.83 MiB | 27.82 MiB |
| 兼容场景 GLB | 127.47 MiB | 127.48 MiB |

全部瓦片、枝干、花叶和草丛数量保留。模型边界保持约 27.856 × 9.490 × 42.341 场景单位。以上缓冲统计不含纹理、浏览器堆和帧缓冲，不能换算为 FPS 提升。

本轮 `src/main.js`、`src/style.css`、`src/lake.js`、`index.html`、`vite.config.js` 与部署工作流均未修改。手机布局、相机、手势、水面和画质设置保留。

## 验证

- 24 项自动测试通过，包括 6 项启动逻辑测试：版本判断、中文/空格/特殊字符路径、依赖缓存和失败、会话复用、停止认证、实际端口地址与关闭清理。
- 脚本 Bash 语法检查通过，启动条件检查 `bash Start-Mac.command --check` 通过。
- 帘幕收束处在系环净空内，6 个系环在多组时刻跟随同一风动公式。
- 两种实际 GLB 的 Khronos 校验均为 0 错误、0 警告，并使用 Three.js 重新导入。跨文件对照 147 组实例采样与 6 幅帘幕全部顶点，形状、颜色和帘幕姿态一致。
- Vite 生产构建通过。导出模块仍按需加载；主包仍保留超过 500 kB 的常规体积提示。

Mac 启动测试在 Linux 上通过脚本入口与模拟服务进行，未启动网页服务；未在真实 Mac 的 Finder/Terminal/Safari 上执行端到端测试，也没有浏览器 WebGL 或真机 FPS 结论。原有 GLB 水面静态近似、环境照明与色调映射差异仍存在；导入完整场景请选择“当前视角”相机。

## 交付

`changyi-pavilion-v4-mac.zip` 包含完整源码、Mac 启停脚本、轻量 GLB、验证报告和 `dist`。压缩包保存脚本的 Unix 执行权限，不包含 `node_modules` 或本地会话记录。兼容 GLB 可单独下载或用 `npm run export:model -- --compatible` 生成，网页运行不依赖它。

未推送 GitHub，也未更新线上页面。
